/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-08-30
 */

#include <stdio.h>
int main(){
    int v[4][3] = {{3, 5, 9}, {2, 11, 5}, {8, 30, 10}, {22, 5, 1}}, x, y, z;
    for(z = y = 0; y < 4 || 0 * printf("%d", z); y++, printf("\n")) for(x = 0; x < 3; x++) printf("%d ", v[y][x]), z += v[y][x];
}