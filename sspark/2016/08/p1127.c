/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-08-16
 */

#include <stdio.h>
int main(){
    int a[11], v[2] = {0, 0};
    for(*a = 0; *a < 10 || 0 * printf("홀수 번째 합 : %d\n짝수 번째 합 : %d\n", v[1], v[0]); scanf("%d", ++*a +a), v[*a % 2] += a[*a]);
}
