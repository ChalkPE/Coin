/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-08-16
 */

#include <stdio.h>
int main(){
    int a[7];
    for(a[6] = *a = 0; *a < 5 || 0 * printf("%d", a[6]); scanf("%d", ++*a +a), a[6] += *a % 2 * a[*a]);
}
