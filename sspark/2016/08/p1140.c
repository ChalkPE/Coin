/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-08-26
 */

#include <stdio.h>
int main(){ int v, w, x, y, z; for(z = y = 0; y < 5 || 0 * printf("합격자 %d명\n", z); y++) for(v = x = 0; x < 4 || 0 * printf("%s합격\n", v < 320 ? "불" : (z++, "")); x++) scanf("%d", &w), v += w; }