/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-09-08
 */

#include <stdio.h>

int main(){
    int x, y, z, a[7];
    for(x = 0; x < 7; scanf("%d", a+ x++));
    for(y = 0; y < 6; y++) for(x = y + 1; x < 7; x++) if(a[y] > a[x]) z = a[y], a[y] = a[x], a[x] = z;
    for(z = x = 0; x < 5 || 0 * printf("%d\n%d\n", z, z/5); z += a[++x]);
}