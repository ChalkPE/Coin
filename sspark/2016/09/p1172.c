/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-09-27
 */

#include <math.h>
#include <stdio.h>

#define swap(a, b) if(a < b) v[3] = a, a = b, b = v[3]

int main(){
    int i, j; double v[4]; for(i = 0; i < 3; scanf("%lf", v+ i++));
    for(i = 0; i < 2 || 0 * printf("%.0lf %.0lf %.0lf\n", ceil(v[0]), floor(v[2]), floor(v[1] + 0.5)); i++) for(j = i + 1; j < 3; j++) swap(v[i], v[j]);
}