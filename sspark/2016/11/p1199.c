/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-11-01
 */

#include <stdio.h>
#include <string.h>

#define read (scanf("%s", s), l += (int) strlen(s))
int main(){ char s[101]; int l = 0; read, read, printf("%d", l); }