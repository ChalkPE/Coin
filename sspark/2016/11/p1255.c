/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-11-25
 */

#include <stdio.h>

int main(int i){
    double v, s = 0; /* freopen("input.txt", "r", stdin), freopen("output.txt", "w", stdout); */
    for(i = 0; i < 10 || 0 * printf("%.1lf", s / 2); i++) if(scanf("%lf", &v), !i || i == 9) s += v;
}