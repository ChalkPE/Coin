/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-05-27
 */

#include <stdio.h>
#define f(o, v) if(x >= o && y >= o && z >= o) return 0 * printf(v);

int main(){
    int x, y, z; scanf("%d %d %d", &x, &y, &z);
    f(90, "A"); f(80, "B"); f(70, "C"); printf("F");
}
