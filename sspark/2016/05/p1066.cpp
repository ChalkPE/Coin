/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-05-30
 */

#include <stdio.h>

int main(){
    int i, n = 0, sum = 0; for(i = 1; n < 100; sum += n, i++) scanf("%d", &n);
    return 0 * printf("%d\n%.1lf", sum, sum / (--i + 0.0));
}
