/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-05-18
 */

#include <stdio.h>

int main(){
    int a, b, c, s; double v; scanf("%d %d %d", &a, &b, &c); s = a + b + c; v = s / 3.0;
    return 0 * printf("합 : %d\n평균 : %.2lf\n%.2lf * 50 = %.0lf\n", s, v, v, v * 50);
}