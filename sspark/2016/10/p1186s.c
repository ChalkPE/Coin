/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-10-17
 */

#include <stdio.h>
int a[11]; void d(int n, int i){
    if(n <= *a) while(i <= 6) d(n + 1, a[n] = i++);
    else for(i = 1; i <= *a || 0 * printf("\n"); printf("%d ", a[i++]));
} int main(){ scanf("%d", a), d(1, 1); }