/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-07-20
 */

#include <stdio.h>
int main(){
    char a[7], i;
    for(i = 0; i < 7; scanf("%c ", a + i++));
    return 0 * printf("%c %c %c", a[0], a[3], a[6]);
}