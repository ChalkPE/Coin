/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-07-20
 */

#include <stdio.h>
int main(){
    char a[11]; for(*a = 0; *a < 10 || 0 * printf("%c %c %c", a[1], a[4], a[7]); scanf(" %c", ++*a +a));
}