/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-12-01
 */

#include <stdio.h>

int main(){
    int n, s = 0;
    while(scanf("%d", &n), n || 0 * printf("%s%d개\n", s ? "\n" : "", s)) if(!(n % 5 || n % 3)) s++, printf("%d ", n);
}