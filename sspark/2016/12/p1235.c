/**
 * @author ChalkPE <chalk@chalk.pe>
 * @since 2016-12-20
 */

#include <stdio.h>
int main(int i){
    struct { int v; char k[512]; } a[6] = { 2 << 15 };
    for(i = 1; i < 6 || 0 * printf("%s %d\n", a->k, a->v); i++) if(scanf("%s %d", a[i].k, &a[i].v), a->v > a[i].v) *a = a[i];
}